# Copy to .env.local and fill in.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional. Leave unset (or set to "supabase") for the real database.
# Set to "mock" only for offline local work with the seeded JSON store.
# NEXT_PUBLIC_DATA_SOURCE=supabase
